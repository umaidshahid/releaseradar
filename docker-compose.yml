version: "3.9"
services:
  backend:
    build: ./backend
    container_name: releaseradar-backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/app
    environment:
      - FIREBASE_CREDENTIALS_PATH=/app/app/firebase.json
    restart: always
